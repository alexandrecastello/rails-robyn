<!DOCTYPE html>
<html>
  <head>
    <meta property="og:description"   content="Ajude a encontrar este pet" />
    <meta property="og:title" content="<%=@pet.species.upcase%> PERDIDO" />
    <meta property="og:image" content="<%= @kit %>" />
    <meta property="og:url"           content="https://www.your-domain.com/your-page.html" />
    <meta property="og:type"          content="website" />
  </head>
  <body>
    <div class="container justify-content-center">
      <strong><h1>Procura-se</h1><strong>
      <%= cl_image_tag @pet.photo.key, class:"card-img-top card-show", width: "400", height: "400" %>
    </div>
    <div>
      <%= @pet.name%>
      <p>Especie: <%= @pet.species  %>
      <p>Descricao: <%= @pet.description %>
      <p>Perdido em: <%= @pet.lost_date.strftime("%d/%b/%y") %>
      <p>Local: <%= @pet.lost_location %>
      <%= @pet.user.email %> 
      
      <p> Viu este pet? Reporte em www.robyn.com.br
    </div>
  </body>
  <p>Criado por @robyn.com.br<p>  
</html>

<html>
<head>
  <title>Your Website Title</title>
    <!-- You can use Open Graph tags to customize link previews.
    Learn more: https://developers.facebook.com/docs/sharing/webmasters -->

</head>

</html>